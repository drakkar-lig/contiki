all: udp-server udp-client relay

UIP_CONF_IPV6=1
UIP_CONF_IPV6_RPL=0

ifndef TARGET
TARGET = sky
endif


CFLAGS += -DPROJECT_CONF_H=\"project-conf.h\"

CFLAGS += -D'RRPL_CONF_IS_COORDINATOR()=1'

CFLAGS += -DWITH_IPV6_RRPL=1
CFLAGS += -DUSE_OPT=1
CFLAGS += -DRRPL_CONF_SND_QRY=1
CFLAGS += -DUIP_CONF_IPV6_RPL=0
#CFLAGS += -DUIP_CONF_ND6_SEND_NA=0

CFLAGS += -RRPL_RANDOM_WAIT=1	

CONTIKI = ../..
ifeq ($(MAKECMDGOALS),udp-server)
  CFLAGS += -DRRPL_CONF_IS_SINK=1
else
  CFLAGS += -DRRPL_CONF_IS_SINK=0
endif

CFLAGS += -DRESOLV_CONF_SUPPORTS_MDNS=0
CFLAGS += -DUIP_CONF_ROUTER=1

include $(CONTIKI)/Makefile.include
